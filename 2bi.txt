fetch('https://jsonplaceholder.typicode.com/users')

.then(resp => resp.json())

.then(arr => arr.map(el => ({
id: el.id,
name: el.name,
username: el.username,
email: el.email,
phone: el.phone
})))

.then(list => console.log(list));

console.log('гатова');